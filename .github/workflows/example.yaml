name: Example
on: [push, pull_request]

jobs:
  kubescape-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: kubescape/kubescape-action@main
        with:
          threshold: "50"
          account: ${{ github.KUBESCAPE_ACCOUNT }}
          

